# Test dependencies (development only — not needed to run the gateway)
pytest>=8.2.0,<9.0.0
pytest-asyncio>=0.23.0,<1.0.0
httpx>=0.27.0,<1.0.0      # already in requirements.txt but explicit here for clarity

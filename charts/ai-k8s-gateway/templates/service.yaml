apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-k8s-gateway.fullname" . }}
  labels:
    {{- include "ai-k8s-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    {{- include "ai-k8s-gateway.selectorLabels" . | nindent 4 }}
